node_modules
dist
.astro
.DS_Store
.env
.env.*
.claude/
.vercel/
.vercel

# Local scratch from the retired research-fleet build (untracked, kept on disk).
/office/
/botnet/
/fleet/

# Working scratch from prior ingest sessions.
codex articles/
sonnet writeups/

# Morning-routine run scratch (worklists, manifests, logs).
.kir-morning/
.kir-indexnow-state.json

# ~175MB generated mentions index — regenerate with tools/build-mentions-index.mjs.
# Must stay OUT of public/: anything there is copied into dist/ and the Vercel
# build output, and one file over 100MB fails the prebuilt upload.
.kir-mentions-index.json
public/article-mentions-index.json
